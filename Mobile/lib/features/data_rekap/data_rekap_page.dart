import 'package:flutter/material.dart';

class DataRekapPage extends StatelessWidget {
  const DataRekapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Data Rekap')));
  }
}
